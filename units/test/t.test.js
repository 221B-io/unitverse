// const Engine = require('../../core/core/lib/engine');
// const t = require('./t');

// const engine = new Engine();å
// engine.register('t', t);

describe('Test - units', () => {
  test('t', async () => {
//     const result = await engine.run('t', {
//       path: 'package.json',
//     });
//     expect(result).toBe('Hello world');
  });
});