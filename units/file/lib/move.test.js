const move = require('./move');

describe('File Move', () => {
  describe('File moves without error', () => {
    test('Promise', () => {

    });
    test('Callback', () => {
      
    });
  });
  test('Source does not exist', () => {

  });
  test('Destination does not exist', () => {

  });
});